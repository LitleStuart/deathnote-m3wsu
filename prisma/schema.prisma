generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  nickname    String  @unique
  description String
  faceitId    String? @unique
}
